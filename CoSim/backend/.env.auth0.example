# Auth0 Backend Configuration
# Add these to your backend .env file or docker-compose environment

# Auth0 Domain (same as frontend)
AUTH0_DOMAIN=your-auth0-domain.auth0.com

# Auth0 API Audience (must match frontend)
AUTH0_AUDIENCE=https://api.cosim.dev

# Auth0 Management API credentials (for user management)
# These are optional if you only need authentication
AUTH0_CLIENT_ID=your-backend-client-id
AUTH0_CLIENT_SECRET=your-backend-client-secret

# JWT Algorithm (RS256 is recommended for Auth0)
AUTH0_ALGORITHMS=RS256

# Issuer URL (constructed from domain)
AUTH0_ISSUER=https://your-auth0-domain.auth0.com/
